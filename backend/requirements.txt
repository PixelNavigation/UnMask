# Requirements for running backend on Render
# Note: For PyTorch, prefer selecting a wheel via the official PyTorch instructions
# (https://pytorch.org/get-started/locally/) depending on CPU/GPU and CUDA version.

Flask>=2.0
flask-cors>=3.0
gunicorn>=20.1

# ML / Deep Learning
torch
torchvision
facenet-pytorch>=2.0
timm>=0.9.2

# Data and utilities
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.2
requests>=2.28

# Image / video processing
Pillow>=9.0
opencv-python-headless>=4.7
albumentations>=1.3.1,<2.0

# Misc
tqdm>=4.64

# Optional (only if used / available)
# If you need CPU-only wheel of torch, install using the PyTorch index before pip installing this file.
